# 数学SRS系统 — 操作手册

---

## 目录

1. [系统概述](#1-系统概述)
2. [环境准备](#2-环境准备)
3. [启动系统](#3-启动系统)
4. [iPad 连接](#4-ipad-连接)
5. [浏览模式](#5-浏览模式)
6. [复习模式](#6-复习模式)
7. [题库模式](#7-题库模式)
8. [导入与导出](#8-导入与导出)
9. [关闭与重启](#9-关闭与重启)
10. [常见问题](#10-常见问题)

---

## 1. 系统概述

**数学SRS** 是一个面向高中数学的间隔重复学习系统，基于 SM-2 记忆算法。

- **运行方式**：Mac 上启动后端服务，iPad/手机/其他设备通过浏览器访问
- **数据存储**：所有数据存储在 Mac 本地 SQLite 数据库中，不上传任何服务器
- **数学渲染**：使用 KaTeX 渲染数学公式
- **卡片格式**：Scheme B 格式，每节一张卡片，含填空(cloze)和选择(choice)两种题型

### 双数据源

| 用途 | 格式 | 说明 |
|------|------|------|
| **浏览** | 纯文本笔记 | 学习概念、例题、知识关联 |
| **复习** | Scheme B 题库 | SRS 间隔重复测试 |

---

## 2. 环境准备

### Mac 端

**必需软件：**

- Python 3.9+（macOS 通常已预装）
- Node.js 18+（[nodejs.org](https://nodejs.org) 下载）

**安装依赖：**

```bash
# 进入项目目录
cd math-srs

# 创建虚拟环境并安装 Python 依赖
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt

# 安装前端依赖
npm install

# 构建前端
npm run build
```

### iPad 端

无需任何安装。使用 Safari 浏览器即可。

---

## 3. 启动系统

在 Mac 终端中执行：

```bash
cd math-srs
env PYTHONPATH=. .venv/bin/uvicorn server.main:app --host 0.0.0.0 --port 3000
```

启动成功后显示：

```
✅ SQLite 初始化完成
✅ 服务启动: http://0.0.0.0:3000
```

**Mac 上访问**：打开浏览器，输入 `http://localhost:3000`

> 如果之前构建过前端但修改了代码，需要重新 `npm run build` 再启动。

---

## 4. iPad 连接

### 步骤

1. 确保 Mac 和 iPad 连接**同一 WiFi**
2. 在 Mac 上查看 IP 地址：
   ```bash
   ipconfig getifaddr en0
   ```
   例如输出 `192.168.1.5`
3. 在 iPad Safari 中访问：`http://192.168.1.5:3000`

### 添加主屏幕快捷方式（推荐）

1. Safari 中打开系统页面
2. 点击分享按钮 → 「添加到主屏幕」
3. 命名为「数学SRS」
4. 之后可以像 App 一样从主屏幕打开

### 离线提示

如果断开了 Mac 或 Mac 关机，iPad 页面顶部会显示黄色横幅：
> ⚠️ 无法连接到后端服务 — 仅浏览模式可用

---

## 5. 浏览模式

浏览模式用于**学习和阅读**，不是测试。

### 5.1 打开浏览模式

点击顶部导航栏「浏览」按钮，或从左侧目录选择章节。

### 5.2 侧边栏目录

```
📐 课本目录

▼ 📖 第一册
  ▼ 第一章 集合与常用逻辑用语    2/3
  
    📖 01-01 集合的概念        ← 可点击
    📖 01-02 集合间的基本关系    ← 可点击
    📖 01-03 集合的基本运算      ← 灰色，内容未制作
```

- 点击 **📖 第一册** → 展开/折叠整册
- 点击 **第一章** → 展开/折叠章节
- 点击 **01-01** 等 → 选中卡片，右侧显示内容
- 灰色卡片表示内容尚未制作
- 左上角 **☰** 按钮 → 折叠/展开侧边栏

### 5.3 维度网格

选中卡片后，显示 7 个内容维度：

| 维度 | 图标 | 内容 |
|------|------|------|
| 核心概念界定 | 📖 | 定义、公式、符号 |
| 解题策略与方法论 | 🧠 | 解题标准步骤(SOP) |
| 易错点与风险规避 | ⚠️ | 常见错误与辨析 |
| 多维原理解码 | 💡 | 代数/几何/逻辑视角 |
| 经典例题精讲 | 🧪 | 例题 + 详细解答 |
| 知识关联网络 | 🔗 | 跨章节知识连接 |
| 101星思维挑战 | 🕸 | 拔高题与拓展 |

### 5.4 查看内容

- **点击维度方格** → 跳转到详情页，全宽显示笔记
- **滚动阅读** → 支持 Markdown 格式（标题、表格、公式）
- **底部导航** → 「← 上一节」「下一节 →」按钮翻页
- **左上角面包屑** → 返回网格视图

### 5.5 切换卡片

- 从左侧目录点击新的章节编号
- 浏览模式会自动切换到该卡片

---

## 6. 复习模式

复习模式是**测试和记忆强化**的核心功能。

### 6.1 进入复习

点击顶部导航栏「复习」按钮。系统自动加载今日待复习的卡片。

### 6.2 复习流程

```
选题页 → 点击维度 → 40秒预览 → 自动进入测试 → 提交评分 → 自动下一维度
```

### 6.3 选题页

- 显示当前卡片标题、进度
- 7 个维度以彩色网格展示
- 已完成的维度显示绿点 ✅ / 红点 ❌
- 全部完成后出现「下一张 →」按钮

### 6.4 预览阶段（40 秒）

- 显示题目内容和正确答案
- 顶部进度条倒计时
- **这是记忆时间**：认真阅读并记住答案
- 倒计时结束后自动进入测试

### 6.5 测试阶段

**填空题 (Cloze)：**

1. 在输入框中填写答案
2. 点击「提交」按钮
3. 全部填空正确 → 得分 3（绿色）
4. 任意空错误 → 得分 0（红色），显示正确答案

**选择题 (Choice)：**

1. 点击选项（选项顺序每次随机）
2. 选中后立即显示结果
3. 选对 → 得分 3（绿色）
4. 选错 → 得分 0（红色），正确选项标绿

### 6.6 评分后

- 评分后自动跳转到下一个未完成的维度
- 无需手动返回选题页
- 点击顶部「← 返回选题」可随时退出

### 6.7 手写草稿板

复习时可以在屏幕上直接演算：

1. 点击底部「🖊 手写草稿」展开画布
2. 用鼠标/触控笔/Apple Pencil 书写
3. 点击「清除」清空画布
4. 切换题目时画布自动清除
5. 再次点击标题栏折叠草稿板

### 6.8 安全限制

测试阶段禁止：
- 粘贴（Cmd+V / 长按粘贴）
- 复制（Cmd+C）
- 右键菜单

### 6.9 复习完成

所有卡片复习完毕显示：
```
🎉 今日复习完成！
没有待复习的卡片
```

---

## 7. 题库模式

查看全部卡片的 SRS 统计信息。

### 7.1 进入题库

点击顶部导航栏「题库」按钮。

### 7.2 统计卡片

顶部三张统计卡片：

| 卡片 | 含义 |
|------|------|
| 总卡片数 | 题库中所有卡片数量 |
| 待复习 | 今日到期需要复习的卡片数 |
| 已掌握 | 尚未到复习日期的卡片数 |

### 7.3 卡片表格

| 列名 | 含义 |
|------|------|
| ID | 卡片编号（如 01-01） |
| 标题 | 卡片名称 |
| 分类 | 所属知识分类 |
| 复习次数 | 累计成功复习次数 |
| 间隔(天) | 当前 SM-2 间隔天数 |
| 难度因子 | ease_factor（≥1.3，越高越容易） |
| 到期日 | 下次复习日期 |
| 状态 | 新卡片(灰) / 待复习(黄) / 已掌握(绿) |

---

## 8. 导入与导出

### 8.1 导入卡片

1. 点击「导入」按钮
2. 在文本框中粘贴 JSON 卡片数据
3. 点击「导入」按钮
4. 显示「✅ 导入 N 张卡片」

**导入格式**：与 `public/data/cards.json` 相同，支持单个对象或数组。

**导入行为**：
- 同 ID 的卡片会被覆盖（INSERT OR REPLACE）
- 被覆盖的卡片旧复习数据会被清除
- 新卡片 source 标记为 "imported"

### 8.2 导出数据

四种导出格式：

| 导出项 | 格式 | 内容 |
|--------|------|------|
| 全部卡片 | JSON | 所有卡片数据 |
| 复习进度 | JSON | srs_state + review_log |
| 复习统计 | CSV | 日期/卡片/维度/评分，可用 Excel 打开 |
| 完整备份 | ZIP | 卡片 + 进度 + 日志三合一 |

点击对应按钮即可下载。

### 8.3 跨设备恢复

1. 在新 Mac 上启动系统
2. 导入之前导出的备份 ZIP 或卡片 JSON
3. 进度文件可单独导入

---

## 9. 关闭与重启

### 关闭

在 Mac 终端中按 `Ctrl + C` 停止服务。

### 重启

```bash
cd math-srs
env PYTHONPATH=. .venv/bin/uvicorn server.main:app --host 0.0.0.0 --port 3000
```

### 重置数据库

如果需要清空所有复习进度重新开始：

```bash
rm data/cards.db data/cards.db-shm data/cards.db-wal
```

下次启动时系统会自动从 `cards.json` 重建题库。

### 更新前端

修改了前端代码后：

```bash
npm run build
# 然后重启后端服务
```

---

## 10. 常见问题

### Q: iPad 打开后显示无法连接？

A: 检查：
1. Mac 和 iPad 是否同一 WiFi
2. Mac 上服务是否在运行（终端应显示 `Uvicorn running on http://0.0.0.0:3000`）
3. IP 地址是否正确（Mac IP 可能变化，重新 `ipconfig getifaddr en0`）
4. Mac 防火墙是否阻止了 3000 端口

### Q: 刷新页面后白屏？

A: 已修复。系统支持 SPA fallback，刷新任意路径均正常。

### Q: 如何在 iPad 上用 Pencil 写字？

A: 复习模式下，点击底部「🖊 手写草稿」展开画布，直接用 Pencil 书写。切换题目自动清空。

### Q: 如何添加新的章节卡片？

A:
1. 在 `public/data/browse/` 创建新的 JSON 文件（浏览内容）
2. 在 `public/data/cards.json` 数组中添加对应卡片（复习题库）
3. 在 `public/data/browse/textbook.json` 中注册章节位置
4. 执行 `npm run build` 重建前端
5. 删除数据库 `rm data/cards.db*` 让系统重新 seed
6. 重启服务

### Q: SM-2 算法如何工作？

A:

| 评分 | 含义 | 效果 |
|------|------|------|
| 3 | 正确 | 间隔延长（1→6→ef×间隔），难度因子 +0.1 |
| 0 | 错误 | 间隔重置为 1 天，难度因子 -0.2 |

难度因子下限 1.3，保证不会无限缩短间隔。

### Q: 数据存在哪里？

A: 所有数据在 `data/cards.db`（SQLite WAL 模式），不上传任何服务器。建议通过导出备份功能定期备份。

### Q: 支持多设备同时复习吗？

A: 支持。多个设备访问同一 Mac 的 3000 端口，共享同一 SQLite 数据库。复习进度实时同步。
