
## 代码审查 (22:47)

对 math-srs 项目进行了全面代码审查，结果写入 `CODE_REVIEW.md`。

发现 17 个问题：
- 🔴 严重 (3): Layout.tsx Mode 类型缺少 'exercises' 导致 tsc 构建失败; LecturesMode.tsx sec 可能 undefined 导致构建失败; books.py 路径穿越安全漏洞
- 🟠 重要 (3): SM-2 多维度复习导致算法叠加（每次维度复习都触发完整 sm2_update）; 评分校验前后端不一致（API 允许 0-3 但 sm2_update 只接受 0/3）; BrowseMode 内联图片渲染丢失周围文本
- 🟡 中等 (7): useEffect 中 setState; 复习 API 调用未 catch; Sidebar any 类型; LecturesMode 空 catch; markdown.tsx Fast Refresh 违规; exercises.py 无 JSON 错误处理; symbols.py 训练接口无输入验证+竞态
- 🟢 轻微 (4): 健康检查加载全部卡片; browse.py 解析 textbook.json; all_dimensions_pass 命名误导; _verify.cjs lint 错误

构建状态: tsc -b 失败 (4 错误), eslint 6 错误, vitest 16/16 通过
